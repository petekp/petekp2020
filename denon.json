{
  "$schema": "https://deno.land/x/denon/schema.json",

  "scripts": {
    "start": {
      "cmd": "deno run --allow-net --allow-read index.ts",
      "desc": "runs index.ts with network and read config"
    }
  }
}
